[project]
name = "movie-recs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
movie-recs = "movie_recs:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
